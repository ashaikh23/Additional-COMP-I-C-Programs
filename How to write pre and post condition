//Precondition: Assumes there is at least one character in the keyboard buffer already.
//Postcondition: The keyboard buffer is clear and the next call to scanf will force a read from the
//keyboard into the keyboard buffer.
void clear_keyboard_buffer(void);
